#[derive(Debug)]
pub enum GeometricalShapes {
    Square,
    Rectangle,
    Triangle,
    Circle,
}

#[derive(Debug)]
pub enum GeometricalVolumes {
    Cube,
    Parallelepiped,
    Sphere,
    Cylinder,
}
